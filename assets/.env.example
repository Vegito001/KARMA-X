# Copy this file to assets/.env.local (or assets/.env) and fill in real
# values. assets/.env* is gitignored, so it never gets committed/pushed.

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_PROJECT_REF=

# Get a free key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
